# Scott Park Portfolio

A performance-first portfolio built with [Astro](https://astro.build/) and Tailwind CSS. It includes the site source, shared styles, and build configuration used to publish scottparkk.github.io. Content lives in the `src` directory, while static assets are stored under `public`. Use this repo to develop locally, preview updates, and deploy the portfolio site.
